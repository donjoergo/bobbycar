# Bpbby Car Controller

Here the Arduino code for the high level Bobby Car Controller is located.

The Controller has the following features:

- Currently it's just reading out the nunchuck input, doing some transforming logic and then sends the commands to the motor driver boards over UART.
